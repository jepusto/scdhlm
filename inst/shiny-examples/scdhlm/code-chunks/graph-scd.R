# Graph 
graph_SCD(design = "{user_design}", 
          data = dat,
          case = {user_case}, phase = {user_phase}, 
          session = {user_session}, outcome = {user_outcome}, 
          {furtherArg_method} {furtherArg_design}
          )

